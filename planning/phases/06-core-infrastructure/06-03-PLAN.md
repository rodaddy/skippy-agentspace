---
phase: 06-core-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - skills/core/SKILL.md
  - INDEX.md
  - .claude-plugin/marketplace.json
  - CLAUDE.md
autonomous: true
requirements: [CORE-05, CORE-06]

must_haves:
  truths:
    - "core/SKILL.md exists under 150 lines with topic sections pointing to references/"
    - "INDEX.md lists the core skill alongside skippy"
    - "marketplace.json includes the core skill in the plugins array"
    - "CLAUDE.md Key Files table references skills/core/"
    - "CORE-05 is explicitly acknowledged as deferred, not silently skipped"
  artifacts:
    - path: "skills/core/SKILL.md"
      provides: "Slim entry point for PAI core infrastructure"
      min_lines: 40
      contains: "name: core"
    - path: "INDEX.md"
      provides: "Updated skill registry with core entry"
      contains: "core"
    - path: ".claude-plugin/marketplace.json"
      provides: "Updated plugin manifest with core skill"
      contains: "core"
  key_links:
    - from: "skills/core/SKILL.md"
      to: "skills/core/references/personas/"
      via: "Personas topic section"
      pattern: "references/personas/"
    - from: "skills/core/SKILL.md"
      to: "skills/core/references/laws/"
      via: "LAWs topic section"
      pattern: "references/laws/"
    - from: "skills/core/SKILL.md"
      to: "skills/core/references/rules/"
      via: "Rules topic section"
      pattern: "references/rules/"
    - from: "skills/core/SKILL.md"
      to: "skills/core/references/templates/"
      via: "Templates topic section"
      pattern: "references/templates/"
    - from: "INDEX.md"
      to: "skills/core/SKILL.md"
      via: "skill registry entry"
      pattern: "core.*SKILL\\.md"
---

<objective>
Create the slim SKILL.md entry point for core and integrate it into the project's skill registry, plugin manifest, and project documentation.

Purpose: CORE-06 ties together all the content from Plans 01 and 02 with a properly structured entry point. This plan also handles CORE-05 (deferred) by explicitly noting it in the SKILL.md rather than silently skipping it. The integration step (INDEX.md, marketplace.json, CLAUDE.md) ensures the core skill is discoverable and installable.

Output: `skills/core/SKILL.md` under 150 lines, updated INDEX.md, updated marketplace.json, updated CLAUDE.md
</objective>

<execution_context>
@/Users/rico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-core-infrastructure/06-CONTEXT.md
@.planning/phases/06-core-infrastructure/06-RESEARCH.md

# Prior plan outputs needed for content inventory
@.planning/phases/06-core-infrastructure/06-01-SUMMARY.md
@.planning/phases/06-core-infrastructure/06-02-SUMMARY.md
</context>

<interfaces>
<!-- Key files this plan reads/modifies. Extracted from codebase. -->

From skills/skippy/SKILL.md (pattern to follow):
```yaml
---
name: skippy
description: Development workflow enhancements -- context awareness, reconciliation, task rigor, plan boundaries, state consistency
metadata:
  version: 0.1.0
  author: Rico
  source: https://github.com/rico/skippy-agentspace
---
```

From INDEX.md (format to extend):
```markdown
| Skill | Path | Commands | References | Use When |
|-------|------|----------|------------|----------|
| skippy | `skippy/SKILL.md` | /skippy:reconcile, ... | context-brackets.md, ... | Development workflow... |
```

From .claude-plugin/marketplace.json (structure to extend):
```json
{
  "plugins": [
    {
      "name": "skippy",
      "skills": ["./skills/skippy"]
    }
  ]
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create core/SKILL.md entry point</name>
  <files>skills/core/SKILL.md</files>
  <action>
Create `skills/core/SKILL.md` following the exact pattern from `skills/skippy/SKILL.md` (87 lines). Target ~80-100 lines (well under the 150-line limit per CONTEXT.md).

Structure (per 06-RESEARCH.md Code Example):

```yaml
---
name: core
description: PAI core infrastructure -- personas, LAWs, rules, and project templates
metadata:
  version: 0.1.0
  author: Rico
  source: https://github.com/rico/skippy-agentspace
---
```

Then topic sections, each with a brief summary and table pointing to reference files:

1. **Personas** section: Table with Persona | Style | Best For | File columns. List all 4 personas. Include switching instructions ("persona bob", "switch to clarisa"). Note that each file is a self-contained prompt fragment.

2. **LAWs** section: Brief "15 mandatory rules. Never violate." intro. Table with # | Name | Enforcement | File columns. List all 15 LAWs with correct enforcement status (hook name or "Manual"). Point to `references/laws/` directory.

3. **Rules** section: Brief intro. Table with Rule | File columns. List all 4 rule files from references/rules/.

4. **Templates** section: Table with Template | Purpose | File columns. List claude-md.template and user.md.template.

5. **Commands** section: Brief note that commands are planned for a future phase (CORE-05 deferred). Something like: "Command packaging deferred. See roadmap Phase 9+ for portable command install." This makes CORE-05 deferral EXPLICIT rather than a silent omission.

Use the SKILL.md from skippy as the structural reference, but DO NOT copy its content -- core has different topic sections (personas/laws/rules/templates vs enhancements/commands).

Count final lines -- must be under 150.
  </action>
  <verify>
    <automated>lines=$(wc -l < skills/core/SKILL.md 2>/dev/null || echo 999); has_frontmatter=$(head -1 skills/core/SKILL.md 2>/dev/null | grep -c "^---"); has_personas=$(grep -c "Personas" skills/core/SKILL.md 2>/dev/null || echo 0); has_laws=$(grep -c "LAWs" skills/core/SKILL.md 2>/dev/null || echo 0); echo "Lines: $lines (max 150), Frontmatter: $has_frontmatter, Personas section: $has_personas, LAWs section: $has_laws"; test "$lines" -lt 150 -a "$has_frontmatter" -eq 1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>skills/core/SKILL.md exists under 150 lines with Agent Skills frontmatter, topic sections for Personas/LAWs/Rules/Templates, and an explicit note that Commands are deferred (CORE-05)</done>
</task>

<task type="auto">
  <name>Task 2: Integrate core skill into INDEX.md, marketplace.json, and CLAUDE.md</name>
  <files>
    INDEX.md
    .claude-plugin/marketplace.json
    CLAUDE.md
  </files>
  <action>
Three integration updates:

**INDEX.md:**
Run `tools/index-sync.sh --generate` to auto-regenerate. If the script does not detect the new core/ skill, manually add a row to the skill table:
```
| core | `core/SKILL.md` | (none -- commands deferred) | personas/, laws/, rules/, templates/ | PAI core infrastructure -- personas, LAWs, rules, and project templates |
```
Verify the generated output includes core alongside skippy.

**marketplace.json:**
Read the existing `.claude-plugin/marketplace.json`. Add a new entry to the `plugins` array for core:
```json
{
  "name": "core",
  "description": "PAI core infrastructure -- personas, LAWs, rules, and project templates",
  "source": "./",
  "strict": false,
  "version": "0.1.0",
  "author": { "name": "Rico" },
  "skills": ["./skills/core"]
}
```
Keep the existing skippy entry unchanged.

**CLAUDE.md:**
Read the existing project CLAUDE.md. Add a row to the Key Files table for core:
```
| Core skill entry point | `skills/core/SKILL.md` |
```
If there's an "Architecture" or similar section, add a brief note that core/ is a peer to skippy/ under skills/.
  </action>
  <verify>
    <automated>index_has_core=$(grep -c "core" INDEX.md 2>/dev/null || echo 0); mp_has_core=$(grep -c '"core"' .claude-plugin/marketplace.json 2>/dev/null || echo 0); claude_has_core=$(grep -c "core" CLAUDE.md 2>/dev/null || echo 0); echo "INDEX.md: $index_has_core refs, marketplace.json: $mp_has_core refs, CLAUDE.md: $claude_has_core refs"; test "$index_has_core" -ge 1 -a "$mp_has_core" -ge 1 -a "$claude_has_core" -ge 1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>INDEX.md lists core skill alongside skippy. marketplace.json has core in plugins array. CLAUDE.md Key Files table references skills/core/SKILL.md. The core skill is fully discoverable and ready for plugin install.</done>
</task>

</tasks>

<verification>
- SKILL.md under 150 lines: `wc -l skills/core/SKILL.md` (expect < 150)
- SKILL.md has valid frontmatter: `head -10 skills/core/SKILL.md` (expect --- block with name: core)
- SKILL.md references all 4 content directories: `grep -c "references/" skills/core/SKILL.md` (expect 4+)
- CORE-05 deferred note present: `grep -i "defer\|future\|command" skills/core/SKILL.md` (expect match)
- INDEX.md has two skills: `grep -c "SKILL.md" INDEX.md` (expect 2)
- marketplace.json valid JSON: `cat .claude-plugin/marketplace.json | python3 -m json.tool > /dev/null 2>&1 && echo VALID || echo INVALID`
- Full directory structure exists: `find skills/core/ -type f | sort`
</verification>

<success_criteria>
- skills/core/SKILL.md exists, under 150 lines, with valid Agent Skills frontmatter
- SKILL.md has topic sections pointing to all 4 reference subdirectories
- CORE-05 deferral is explicitly noted (not silently skipped)
- INDEX.md, marketplace.json, and CLAUDE.md all reference the core skill
- marketplace.json remains valid JSON
- The core skill is structurally ready for `tools/index-sync.sh` and plugin install
</success_criteria>

<output>
After completion, create `.planning/phases/06-core-infrastructure/06-03-SUMMARY.md`
</output>
