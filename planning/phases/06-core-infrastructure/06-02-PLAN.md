---
phase: 06-core-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/core/references/rules/communication-style.md
  - skills/core/references/rules/stack-preferences.md
  - skills/core/references/rules/output-locations.md
  - skills/core/references/rules/minimal-claude-dir.md
  - skills/core/references/templates/claude-md.template
  - skills/core/references/templates/user.md.template
autonomous: true
requirements: [CORE-03, CORE-04]

must_haves:
  truths:
    - "Communication style and operational rules exist as public-safe reference docs"
    - "A CLAUDE.md template exists that a new project can copy and customize"
    - "A user.md template exists showing the structure for user context"
    - "No private content (IPs, server names, personal details) appears in any public file"
  artifacts:
    - path: "skills/core/references/rules/communication-style.md"
      provides: "Public-safe communication style conventions"
      min_lines: 20
    - path: "skills/core/references/rules/stack-preferences.md"
      provides: "Tool and stack defaults (bun, uv, brew)"
      min_lines: 15
    - path: "skills/core/references/templates/claude-md.template"
      provides: "Opinionated CLAUDE.md project starter"
      min_lines: 60
    - path: "skills/core/references/templates/user.md.template"
      provides: "User context structure template"
      min_lines: 25
  key_links:
    - from: "skills/core/references/templates/claude-md.template"
      to: "skills/core/references/laws/"
      via: "LAW references section"
      pattern: "LAW|laws/"
    - from: "skills/core/references/templates/claude-md.template"
      to: "skills/core/references/personas/"
      via: "persona default placeholder"
      pattern: "persona|CUSTOMIZE"
---

<objective>
Extract style/communication rules and create project templates as portable reference docs under the core skill structure.

Purpose: CORE-03 packages operational conventions for public distribution. CORE-04 creates the opinionated CLAUDE.md template that makes bootstrapping new projects fast. Both are independent of the persona and LAW work in Plan 01.

Output: 4 rule files under `skills/core/references/rules/` and 2 template files under `skills/core/references/templates/`
</objective>

<execution_context>
@/Users/rico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-core-infrastructure/06-CONTEXT.md
@.planning/phases/06-core-infrastructure/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public-safe rule files</name>
  <files>
    skills/core/references/rules/communication-style.md
    skills/core/references/rules/stack-preferences.md
    skills/core/references/rules/output-locations.md
    skills/core/references/rules/minimal-claude-dir.md
  </files>
  <action>
Read the source rule files:
- `~/.config/pai-private/rules/style/communication-style.md` (28 lines)
- `~/.config/pai-private/rules/stack/python-preferences.md` (27 lines)
- `~/.config/pai-private/rules/stack/typescript-preferences.md` (33 lines)

Also read `~/.claude/CLAUDE.md` for output-locations and minimal-claude-dir patterns (these are in the main CLAUDE.md as inline rules).

Create `skills/core/references/rules/` directory and write 4 rule files:

**communication-style.md** (~30-40 lines):
- Extract from communication-style.md source
- Persona-aware styling table (Skippy: sarcastic, Bob: explanatory, Clarisa: warm, April: playful)
- What NOT to do rules (no filler, no repeating questions, no disclaimers)
- No em dashes rule
- Reference personas by name but do NOT embed persona definitions (those live in personas/)

**stack-preferences.md** (~30-40 lines):
- Merge python-preferences.md and typescript-preferences.md into one file
- Package manager defaults: bun (Node.js), uv (Python), brew (macOS)
- "never npm/yarn/pip" rule
- `claudePy` for ad-hoc Python
- Stack selection conventions

**output-locations.md** (~20-30 lines):
- Reports go to `Development/.reports/<project-name>/` (centralized, no dates, overwrite)
- Never ~ or /tmp for reports
- Never `<project>/.reports/`
- Genericize any PAI-specific paths to show the PATTERN, not private values

**minimal-claude-dir.md** (~15-20 lines):
- ~/.claude/ should contain minimal content: symlinks only
- Source of truth is external config directory
- Show the pattern without hardcoding specific private paths

CRITICAL: Create PUBLIC-SAFE versions. The source files in `~/.config/pai-private/` may contain private content (IP addresses, infrastructure details). Extract the PATTERN and CONVENTION, not private values. Use placeholder syntax where values are user-specific.
  </action>
  <verify>
    <automated>count=$(ls skills/core/references/rules/*.md 2>/dev/null | wc -l | tr -d ' '); private=$(grep -l "10\.71\.\|192\.168\.\|rico@" skills/core/references/rules/*.md 2>/dev/null | wc -l | tr -d ' '); echo "Rule files: $count (expect 4), Private leaks: $private (expect 0)"; test "$count" -eq 4 -a "$private" -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>4 rule files exist under skills/core/references/rules/, all public-safe (no IP addresses, server names, or personal details), each documents the convention pattern with customizable defaults</done>
</task>

<task type="auto">
  <name>Task 2: Create CLAUDE.md and user.md templates</name>
  <files>
    skills/core/references/templates/claude-md.template
    skills/core/references/templates/user.md.template
  </files>
  <action>
Create `skills/core/references/templates/` directory and write 2 template files.

**claude-md.template** (~80-100 lines per CONTEXT.md locked decision):

Use HTML comment placeholder syntax (per 06-RESEARCH.md Pattern 4):
```
<!-- CUSTOMIZE: description (default: value) -->
```
with the actual default value inline in the text.

Must include these sections (per CONTEXT.md locked decision):
1. Project header with stack placeholders
2. LAW references -- point to core/references/laws/ or summarize the 15 LAWs with a reference
3. Default persona selection: `<!-- CUSTOMIZE: default_persona (default: skippy) -->`
4. Stack preferences with defaults: bun, uv, brew (with CUSTOMIZE comments showing they're changeable)
5. Skills-first reminder (check skills before manual work)
6. Verification loop (Boris pattern -- run tests/build after implementation)
7. Key Files table (with placeholder rows)
8. Corrections section (empty starter)

The template must be OPINIONATED -- PAI defaults baked in, but clearly customizable via CUSTOMIZE comments. Not a minimal scaffold.

Include cascade note: project CLAUDE.md persona overrides global default persona.

**user.md.template** (~30-40 lines):

Follow the structure from 06-RESEARCH.md Code Examples section:
1. Identity section (name, role)
2. Preferences section (editor, shell, OS)
3. Communication Style section (default persona, verbosity, expertise domains)
4. Context section (current focus, infrastructure, constraints)

Each section uses HTML comment CUSTOMIZE instructions.
Add a header comment: "This file contains personal context. NEVER commit to a public repository."

This is NEW content (no existing source to extract from). Follow the user.md concept from CONTEXT.md -- it's a first-class core concept inspired by OpenClaw's USER.md.
  </action>
  <verify>
    <automated>claude_lines=$(wc -l < skills/core/references/templates/claude-md.template 2>/dev/null || echo 0); user_lines=$(wc -l < skills/core/references/templates/user.md.template 2>/dev/null || echo 0); customize=$(grep -c "CUSTOMIZE" skills/core/references/templates/claude-md.template 2>/dev/null || echo 0); echo "claude-md.template: $claude_lines lines (target 80-100), user.md.template: $user_lines lines, CUSTOMIZE markers: $customize"; test "$claude_lines" -ge 60 -a "$user_lines" -ge 25 -a "$customize" -ge 3 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>claude-md.template exists at 80-100 lines with opinionated PAI defaults and CUSTOMIZE placeholders for stack/persona/settings. user.md.template exists with Identity/Preferences/Communication/Context sections and privacy header.</done>
</task>

</tasks>

<verification>
- All 4 rule files exist: `ls skills/core/references/rules/*.md`
- Both templates exist: `ls skills/core/references/templates/*`
- No private content leaked: `grep -r "10\.71\.\|192\.168\.\|rico@\|ThunderBolt" skills/core/references/rules/ skills/core/references/templates/` (should return nothing)
- CLAUDE.md template has placeholder syntax: `grep "CUSTOMIZE" skills/core/references/templates/claude-md.template`
- CLAUDE.md template references LAWs and personas: `grep -i "law\|persona" skills/core/references/templates/claude-md.template`
</verification>

<success_criteria>
- 4 rule files exist, all public-safe, each documenting a convention with customizable defaults
- claude-md.template is 80-100 lines, opinionated, with CUSTOMIZE placeholders
- user.md.template exists with privacy header and structured sections
- No private content in any file
- Directory structure: skills/core/references/rules/ and skills/core/references/templates/
</success_criteria>

<output>
After completion, create `.planning/phases/06-core-infrastructure/06-02-SUMMARY.md`
</output>
