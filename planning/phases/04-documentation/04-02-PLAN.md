---
phase: 04-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - STRU-02

must_haves:
  truths:
    - "CLAUDE.md includes origin story (PAUL analysis, what was stolen/rejected)"
    - "CLAUDE.md includes architectural decisions table (parasitic skill chosen over alternatives)"
    - "CLAUDE.md includes current project status (all 4 phases complete)"
    - "CLAUDE.md provides enough context for a cold session to orient without asking questions"
    - "CLAUDE.md is self-contained -- doesn't require reading .planning/ to understand the project"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Cold session context document"
      contains: "origin"
---

<objective>
Rewrite CLAUDE.md to be a complete cold-session orientation document. A new Claude Code session opening this repo should immediately understand: what this is, why it exists, how it's structured, what decisions were made, and what the current state is.

The current CLAUDE.md has good bones but needs: origin story polish, architectural decisions context, updated status (all phases complete), and enough prose that reading .planning/ isn't required to get started.

Output: CLAUDE.md that serves as a standalone project brief for any new session.
</objective>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@skills/skippy/SKILL.md
@INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md as cold-session brief</name>
  <files>CLAUDE.md</files>
  <action>
Read the current CLAUDE.md and all context files. Rewrite CLAUDE.md to include:

1. **Header** -- project name, one-line description, LAWs reference
2. **What This Is** -- clear explanation (portable Claude Code skill repo, cherry-picks from GSD + PAUL)
3. **Why This Exists** -- origin story: analyzed PAUL framework, found 5 ideas worth stealing, rejected others. Include the "What We Stole" and "What We Rejected" tables.
4. **Architecture** -- parasitic skill approach (chosen over full framework and GSD patching). Brief table of 3 options evaluated.
5. **What's Built** -- updated file tree showing all directories including .claude-plugin/
6. **The 5 Enhancements** -- table of what was taken from PAUL with reference file paths
7. **Commands** -- the 3 skippy commands with one-line descriptions
8. **Installation** -- plugin install AND manual install instructions
9. **Upstream Sources** -- GSD and PAUL repos with what we take from each
10. **Constraints** -- portability, self-contained, no build step, no GSD modification, shell+markdown only
11. **Key Files** -- quick reference table for .planning/ files (for developers, not cold sessions)

Keep it under 150 lines. Dense, not padded. Every section earns its place.

Update the status to reflect all 4 phases complete. Remove the "Known issues" section and "Next action" line since all phases are done.
  </action>
  <verify>
    <automated>wc -l < CLAUDE.md | xargs -I{} test {} -le 150 && grep -q 'origin\|Origin\|PAUL' CLAUDE.md && grep -q 'parasitic\|Parasitic\|Architecture\|architecture' CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CLAUDE.md is a self-contained cold-session brief under 150 lines with origin story, architecture decisions, current status, and installation instructions.</done>
</task>

</tasks>
