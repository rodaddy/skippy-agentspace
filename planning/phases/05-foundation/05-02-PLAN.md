---
phase: 05-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - upstreams/gsd/upstream.json
  - upstreams/paul/upstream.json
  - skills/skippy/.versions
autonomous: true
requirements:
  - FOUN-02
  - FOUN-03
  - FOUN-04

must_haves:
  truths:
    - "Running ls upstreams/ shows one directory per tracked upstream (gsd, paul)"
    - "Each upstream directory contains a valid upstream.json with repo URL, branch, and SHA"
    - "Adding a new upstream requires only creating a new directory with upstream.json -- no code changes"
    - "The old .versions file is removed with no data loss"
  artifacts:
    - path: "upstreams/gsd/upstream.json"
      provides: "GSD upstream tracking metadata"
      contains: "gsd-build/get-shit-done"
    - path: "upstreams/paul/upstream.json"
      provides: "PAUL upstream tracking metadata"
      contains: "ChristopherKahler/paul"
  key_links:
    - from: "upstreams/gsd/upstream.json"
      to: "https://github.com/gsd-build/get-shit-done.git"
      via: "repo field"
      pattern: "gsd-build/get-shit-done"
    - from: "upstreams/paul/upstream.json"
      to: "https://github.com/ChristopherKahler/paul.git"
      via: "repo field"
      pattern: "ChristopherKahler/paul"
---

<objective>
Create the extensible upstream registry and migrate from the old .versions tracking format.

Purpose: Replace the hardcoded GSD+PAUL tracking in `.versions` (shell key=value) with a directory-per-upstream JSON registry under `upstreams/`. This is the foundation for Phase 8's generic upstream checker.

Output: `upstreams/gsd/upstream.json`, `upstreams/paul/upstream.json`, removal of `skills/skippy/.versions`.
</objective>

<execution_context>
@/Users/rico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation/05-CONTEXT.md
@.planning/phases/05-foundation/05-RESEARCH.md
@skills/skippy/.versions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create upstream registry with GSD and PAUL entries</name>
  <files>upstreams/gsd/upstream.json, upstreams/paul/upstream.json</files>
  <action>
Create the `upstreams/` directory structure at repo root with two subdirectories.

**Create `upstreams/gsd/upstream.json`:**
```json
{
  "name": "gsd",
  "description": "Get Shit Done -- phased execution framework for Claude Code",
  "repo": "https://github.com/gsd-build/get-shit-done.git",
  "branch": "main",
  "last_checked_sha": "none",
  "last_check": "never",
  "cherry_picks": [],
  "notes": "Base infrastructure -- we augment, never modify"
}
```

**Create `upstreams/paul/upstream.json`:**
```json
{
  "name": "paul",
  "description": "Plan-Apply-Unify Loop -- structured planning framework",
  "repo": "https://github.com/ChristopherKahler/paul.git",
  "branch": "main",
  "last_checked_sha": "none",
  "last_check": "never",
  "cherry_picks": [
    "context-brackets",
    "mandatory-reconciliation",
    "task-anatomy",
    "plan-boundaries",
    "state-consistency"
  ],
  "notes": "5 ideas cherry-picked as reference docs in skills/skippy/references/"
}
```

Both files use the schema documented in CONVENTIONS.md (created by plan 05-01, but this plan does not depend on it -- the schema is defined in RESEARCH.md).

The `last_checked_sha` and `last_check` values match the current `.versions` data (both are "none"/"never"), preserving the existing state. PAUL's `cherry_picks` list the 5 ideas already extracted and documented in `skills/skippy/references/`.

Validate both files parse as valid JSON using `jq`.
  </action>
  <verify>
    <automated>jq . upstreams/gsd/upstream.json > /dev/null 2>&1 && jq . upstreams/paul/upstream.json > /dev/null 2>&1 && jq -e '.repo' upstreams/gsd/upstream.json > /dev/null && jq -e '.repo' upstreams/paul/upstream.json > /dev/null && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>upstreams/gsd/upstream.json and upstreams/paul/upstream.json exist, both parse as valid JSON, both contain repo URL, branch, and last_checked_sha fields</done>
</task>

<task type="auto">
  <name>Task 2: Remove old .versions file and validate extensibility</name>
  <files>skills/skippy/.versions</files>
  <action>
**Remove the old tracking file:**
Delete `skills/skippy/.versions`. Per CONTEXT.md: ".versions file removed after upstreams/gsd/upstream.json and upstreams/paul/upstream.json are created."

Note: `skippy-update.sh` will still run -- it initializes `.versions` if missing (line 17-21). It will reset to `none/none/never`, which is identical to the current state. Phase 8 will rewrite the script to read `upstreams/*/upstream.json` instead. Do NOT modify `skippy-update.sh` in this phase.

**Validate extensibility (FOUN-03):**
Run a quick smoke test to prove adding a new upstream requires only creating a directory:
1. Create `upstreams/test-upstream/upstream.json` with minimal valid content
2. Verify `ls upstreams/` shows three directories (gsd, paul, test-upstream)
3. Remove `upstreams/test-upstream/` (cleanup -- this was just a validation)

This proves the directory-as-registry pattern works without code changes.

**Stage the .versions removal for git:**
Use `git rm skills/skippy/.versions` to properly track the deletion.
  </action>
  <verify>
    <automated>test ! -f skills/skippy/.versions && ls upstreams/gsd/upstream.json > /dev/null 2>&1 && ls upstreams/paul/upstream.json > /dev/null 2>&1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>.versions file is removed from skills/skippy/. Both upstream.json files exist. Extensibility validated by temporary test-upstream directory creation and removal. No scripts were modified.</done>
</task>

</tasks>

<verification>
- `ls upstreams/` shows `gsd/` and `paul/` directories
- `jq . upstreams/gsd/upstream.json` parses without error
- `jq . upstreams/paul/upstream.json` parses without error
- `skills/skippy/.versions` does not exist
- No files in `skills/skippy/scripts/` were modified
- Extensibility test passed (new upstream = new directory only)
</verification>

<success_criteria>
- `ls upstreams/gsd/upstream.json upstreams/paul/upstream.json` succeeds
- `jq -e '.repo' upstreams/gsd/upstream.json` returns the GSD repo URL
- `jq -e '.cherry_picks | length' upstreams/paul/upstream.json` returns 5
- `test ! -f skills/skippy/.versions` passes
- `ls skills/skippy/scripts/skippy-update.sh` still exists (not modified)
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation/05-02-SUMMARY.md`
</output>
