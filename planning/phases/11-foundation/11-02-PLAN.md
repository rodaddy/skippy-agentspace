---
phase: 11-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - tools/verify.sh
  - tools/validate-hooks.sh
  - tools/prereqs.sh
  - tools/index-sync.sh
  - tools/install.sh
  - tools/uninstall.sh
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "All 6 tools/ scripts source common.sh with graceful fallback if the file is missing"
    - "No skill scripts (skills/*/scripts/*.sh) source common.sh -- they remain standalone"
    - "Scripts work identically after migration -- same output, same exit codes"
    - "prereqs.sh fallback stubs use Bash 3.2-compatible syntax only"
  artifacts:
    - path: "tools/verify.sh"
      provides: "Health check script using skippy_ functions"
      contains: "skippy_pass"
    - path: "tools/validate-hooks.sh"
      provides: "Hook validation using skippy_ functions"
      contains: "skippy_pass"
    - path: "tools/prereqs.sh"
      provides: "Prerequisite checker using skippy_ functions with Bash 3.2 fallback"
      contains: "skippy_pass"
    - path: "tools/index-sync.sh"
      provides: "Index sync using skippy_repo_root and skippy_is_installed"
      contains: "skippy_repo_root"
    - path: "tools/install.sh"
      provides: "Installer using skippy_repo_root"
      contains: "skippy_repo_root"
    - path: "tools/uninstall.sh"
      provides: "Uninstaller using skippy_repo_root"
      contains: "skippy_repo_root"
  key_links:
    - from: "tools/verify.sh"
      to: "tools/lib/common.sh"
      via: "source command with fallback"
      pattern: "source.*lib/common\\.sh"
    - from: "tools/validate-hooks.sh"
      to: "tools/lib/common.sh"
      via: "source command with fallback"
      pattern: "source.*lib/common\\.sh"
    - from: "tools/prereqs.sh"
      to: "tools/lib/common.sh"
      via: "source command with Bash 3.2 fallback"
      pattern: "source.*lib/common\\.sh"
---

<objective>
Migrate all 6 tools/ scripts to source common.sh, replacing duplicated patterns with skippy_-namespaced shared functions.

Purpose: Complete the DRY extraction by updating each script to use the shared library from Plan 01. Each script gets a source-with-fallback block near the top, and all existing duplicated functions/variables are replaced with their skippy_ equivalents. The migration is all-or-nothing per script to prevent double-counting (Research pitfall #4).

Output: All 6 tools/ scripts updated, no behavioral changes, same output and exit codes.
</objective>

<execution_context>
@/Users/rico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundation/11-CONTEXT.md
@.planning/phases/11-foundation/11-RESEARCH.md
@.planning/phases/11-foundation/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: tools/lib/common.sh provides these functions -->
skippy_repo_root()      -- echoes repo root path, validates via skills/ directory
skippy_pass(msg)        -- "  PASS: $msg", increments SKIPPY_PASS
skippy_warn(msg)        -- "  WARN: $msg", increments SKIPPY_WARN
skippy_fail(msg)        -- "  FAIL: $msg", increments SKIPPY_FAIL
skippy_suggest(msg)     -- "    Fix: $msg"
skippy_section(name)    -- "=== $name ==="
skippy_summary()        -- prints totals, returns 0 if no failures / 1 if failures
skippy_is_installed(nm) -- returns 0 if skill symlinked
Variables: SKIPPY_PASS, SKIPPY_WARN, SKIPPY_FAIL (initialized to 0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate verify.sh, validate-hooks.sh, and prereqs.sh</name>
  <files>tools/verify.sh, tools/validate-hooks.sh, tools/prereqs.sh</files>
  <action>
These 3 scripts are the "heavy" migrations -- they all define their own pass/fail/counter systems that must be fully replaced.

**verify.sh** (heaviest user -- uses all 5 function groups):
1. Add source-with-fallback block after `set -euo pipefail`. Use full fallback stubs (all functions + counters).
2. Replace `REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"` with `REPO_ROOT="$(skippy_repo_root)"`.
3. Remove local function definitions: `pass()`, `warn()`, `fail()`, `suggest()` (lines ~30-33).
4. Remove counter initialization: `PASS_COUNT=0`, `WARN_COUNT=0`, `FAIL_COUNT=0` (lines ~26-28).
5. Replace ALL calls: `pass ` -> `skippy_pass `, `warn ` -> `skippy_warn `, `fail ` -> `skippy_fail `, `suggest ` -> `skippy_suggest `.
6. Replace `echo "=== ... ==="` section headers with `skippy_section "..."`.
7. Replace the summary block at the end (lines ~248-254) with `skippy_summary`.
8. Remove manual exit-code logic at the end -- `skippy_summary` handles it.

**validate-hooks.sh** (pass/fail counters, no warn):
1. Add source-with-fallback block after `set -euo pipefail`. Use full fallback stubs.
2. Replace `REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"` with `REPO_ROOT="$(skippy_repo_root)"`. Keep `SCRIPT_DIR` since it's used for other purposes.
3. Remove local `pass()` and `fail()` function definitions (lines ~42-49).
4. Remove `PASS_COUNT=0` and `FAIL_COUNT=0` (lines ~33-34).
5. Replace ALL calls: `pass ` -> `skippy_pass `, `fail ` -> `skippy_fail `.
6. Replace `echo "=== ... ==="` section headers with `skippy_section "..."` if present.
7. Replace the results summary block at the end (lines ~309-317) with `skippy_summary`. The current format is `Results: $PASS_COUNT/$TOTAL passed, $FAIL_COUNT failed` -- skippy_summary uses a different format but provides consistent behavior. Remove the manual TOTAL computation and exit-code logic.

**prereqs.sh** (Bash 3.2 fallback required):
1. Add source-with-fallback block after `set -euo pipefail`. CRITICAL: The fallback stubs MUST use Bash 3.2-compatible syntax only. No associative arrays, no `${var,,}`, no `|&`, no `mapfile`. Standard `[[ ]]` and `$(( ))` arithmetic are fine.
2. Replace `ok_count=0` / `missing_count=0` with reliance on `SKIPPY_PASS` / `SKIPPY_FAIL` from common.sh.
3. Replace `report_ok()` calls with `skippy_pass`. Replace `report_missing()` calls with `skippy_fail`. Replace `report_outdated()` calls with `skippy_warn`.
4. Remove the `report_ok()`, `report_missing()`, `report_outdated()` function definitions (lines ~20-33).
5. Per Research open question #1: Keep the final recount pattern (`final_ok`/`final_missing` and boolean flags like `git_ok`, `bash_ok`). prereqs.sh has an interactive install-prompt flow where initial scan results can change. Use skippy_ helpers for display but keep prereqs.sh's own exit-code logic rather than replacing with `skippy_summary`.
6. Do NOT add a `REPO_ROOT` line -- prereqs.sh does not currently use REPO_ROOT and does not need it.

For ALL three scripts: The fallback block must be the SAME copy-pasteable block (per CONTEXT.md specifics). The only exception is prereqs.sh which must additionally avoid Bash 4+ syntax in the fallback. Since the standard fallback block already avoids Bash 4+ syntax (uses only `[[ ]]`, `$(( ))`, `echo`), the SAME block works for all scripts.
  </action>
  <verify>
    <automated>cd /Volumes/ThunderBolt/Development/skippy-agentspace && bash tools/verify.sh && bash tools/validate-hooks.sh && bash tools/prereqs.sh < /dev/null</automated>
  </verify>
  <done>All 3 scripts source common.sh with fallback, use skippy_ function names exclusively (no bare pass/fail/warn), produce consistent output format, and exit with correct codes. No Bash 4+ syntax in prereqs.sh fallback block.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate index-sync.sh, install.sh, and uninstall.sh</name>
  <files>tools/index-sync.sh, tools/install.sh, tools/uninstall.sh</files>
  <action>
These 3 scripts are "light" migrations -- they only need repo root and/or install detection, not the full pass/fail/counter system.

**index-sync.sh** (uses REPO_ROOT + is_installed):
1. Add source-with-fallback block after `set -euo pipefail`. Use a minimal fallback containing only `skippy_repo_root()` and `skippy_is_installed()` (this script doesn't use pass/fail/counters).
2. Replace `REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"` with `REPO_ROOT="$(skippy_repo_root)"`.
3. Replace the local `is_installed()` function definition (lines ~22-28) with usage of `skippy_is_installed()`.
4. Replace all `is_installed ` calls with `skippy_is_installed `.

**install.sh** (uses REPO_ROOT, inline symlink checks):
1. Add source-with-fallback block after `set -euo pipefail`. Use a minimal fallback containing only `skippy_repo_root()` and `skippy_is_installed()`.
2. Replace `REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"` with `REPO_ROOT="$(skippy_repo_root)"`.
3. If install.sh has inline symlink existence checks (like `[[ -L "$HOME/.claude/skills/$name" ]]`), replace them with `skippy_is_installed "$name"` calls where appropriate. Only replace the detection pattern -- leave the actual symlink creation logic untouched.

**uninstall.sh** (uses REPO_ROOT derivation in --all mode):
1. Add source-with-fallback block after `set -euo pipefail`. Use a minimal fallback containing only `skippy_repo_root()`.
2. uninstall.sh currently derives `REPO_SKILLS_DIR` inline for its `--all` safety check (verifying symlinks point to this repo before removing). Add `REPO_ROOT="$(skippy_repo_root)"` and derive `REPO_SKILLS_DIR="$REPO_ROOT/skills"` from it, replacing any inline derivation.
3. Do NOT replace uninstall.sh's `list_installed()` function -- that lists installed skills for display, which is different from `skippy_is_installed()` which checks a single skill.

For the minimal fallback blocks: Since these scripts don't use pass/fail/counters, use a reduced fallback that only defines `skippy_repo_root()` and `skippy_is_installed()` (where needed). In the fallback, use `$0` (not `$BASH_SOURCE`) for path derivation since the fallback runs inline in the caller.

Final verification: Run `grep -r "common.sh" skills/` to confirm zero matches -- skill scripts must NOT source common.sh per the portability constraint.
  </action>
  <verify>
    <automated>cd /Volumes/ThunderBolt/Development/skippy-agentspace && bash tools/index-sync.sh --check && bash tools/install.sh 2>&1 | head -5 && bash tools/uninstall.sh 2>&1 | head -5 && echo "---" && grep -r "common.sh" skills/ ; echo "skill grep exit: $?"</automated>
  </verify>
  <done>All 3 scripts source common.sh with minimal fallback, use skippy_repo_root and skippy_is_installed where applicable, produce identical behavior to before migration. grep -r "common.sh" skills/ returns zero matches.</done>
</task>

</tasks>

<verification>
- All 6 tools/ scripts run without error: `for f in tools/*.sh; do echo "--- $f ---"; bash "$f" --help 2>/dev/null || bash "$f" < /dev/null 2>&1 | tail -3; done`
- `bash tools/verify.sh` passes with correct pass/warn/fail counts
- `bash tools/validate-hooks.sh` passes with correct results
- `bash tools/prereqs.sh < /dev/null` runs with correct output formatting
- `grep -r "common.sh" skills/` returns no matches (portability constraint)
- `grep -c "skippy_" tools/verify.sh` returns > 0 (migration happened)
- Fallback test: `mv tools/lib/common.sh /tmp/ && bash tools/verify.sh; mv /tmp/common.sh tools/lib/` still works with degraded output
</verification>

<success_criteria>
- All 6 scripts source common.sh near the top with graceful fallback
- No bare pass()/fail()/warn() function definitions remain in any script
- No bare PASS_COUNT/FAIL_COUNT/WARN_COUNT variables remain
- No bare is_installed() definitions remain (replaced by skippy_is_installed)
- prereqs.sh fallback block uses only Bash 3.2-compatible syntax
- All scripts produce same functional output and exit codes as before
- Zero references to common.sh in skills/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation/11-02-SUMMARY.md`
</output>
