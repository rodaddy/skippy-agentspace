---
phase: 08-upstream-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - upstreams/omc/upstream.json
  - docs/cross-package-analysis.md
autonomous: true
requirements:
  - UPST-01
  - UPST-02

must_haves:
  truths:
    - "upstreams/omc/ exists with valid upstream.json matching the established schema"
    - "A cross-package analysis document exists with pattern comparisons across GSD, PAUL, and OMC"
    - "All 37 OMC skills are categorized as cherry-pick, reject (with reason), or defer"
    - "Every shared pattern has a side-by-side comparison table with explicit recommendation"
  artifacts:
    - path: "upstreams/omc/upstream.json"
      provides: "OMC upstream registry entry"
      contains: "oh-my-claudecode"
    - path: "docs/cross-package-analysis.md"
      provides: "Cross-package pattern analysis with OMC inventory"
      min_lines: 200
  key_links:
    - from: "upstreams/omc/upstream.json"
      to: "upstreams/gsd/upstream.json"
      via: "same schema fields"
      pattern: "last_checked_sha"
    - from: "docs/cross-package-analysis.md"
      to: "skills/skippy/references/"
      via: "Reference doc column links to best-of-breed docs"
      pattern: "references/"
---

<objective>
Register OMC as the third tracked upstream and create the cross-package pattern analysis document that drives all subsequent work in this phase.

Purpose: UPST-01 establishes OMC in the upstream registry so /skippy:update can track it. UPST-02 produces the analysis document that identifies which patterns deserve best-of-breed reference docs (Plan 02) and confirms the OMC cherry-pick inventory.
Output: upstreams/omc/upstream.json + docs/cross-package-analysis.md
</objective>

<execution_context>
@/Users/rico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-upstream-analysis/08-CONTEXT.md
@.planning/phases/08-upstream-analysis/08-RESEARCH.md

<interfaces>
<!-- Existing upstream.json schema from CONVENTIONS.md -->
From upstreams/gsd/upstream.json:
```json
{
  "name": "gsd",
  "description": "Get Shit Done -- phased execution framework for Claude Code",
  "repo": "https://github.com/gsd-build/get-shit-done.git",
  "branch": "main",
  "last_checked_sha": "none",
  "last_check": "never",
  "cherry_picks": [],
  "notes": "Base infrastructure -- we augment, never modify"
}
```

From upstreams/paul/upstream.json:
```json
{
  "name": "paul",
  "description": "Plan-Apply-Unify Loop -- structured planning framework",
  "repo": "https://github.com/ChristopherKahler/paul.git",
  "branch": "main",
  "last_checked_sha": "none",
  "last_check": "never",
  "cherry_picks": ["context-brackets", "mandatory-reconciliation", "task-anatomy", "plan-boundaries", "state-consistency"],
  "notes": "5 ideas cherry-picked as reference docs in skills/skippy/references/"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register OMC as third upstream</name>
  <files>upstreams/omc/upstream.json</files>
  <action>
Create `upstreams/omc/` directory and `upstream.json` following the exact schema from CONVENTIONS.md (same fields as gsd and paul entries).

Values:
- name: "omc"
- description: "Oh My ClaudeCode -- multi-agent orchestration with skills, hooks, and execution modes"
- repo: "https://github.com/Yeachan-Heo/oh-my-claudecode.git"
- branch: "main"
- last_checked_sha: "96a5d3725586b18492fb4b8019ec37ba6ffd7b14" (current as of 2026-03-07 research)
- last_check: "2026-03-07"
- cherry_picks: [] (will be populated after Plan 02 creates reference docs)
- notes: "Cherry-pick ideas only -- no runtime dependency on OMC's Node.js infrastructure"

Validate with `jq .` to confirm valid JSON.
  </action>
  <verify>
    <automated>cat upstreams/omc/upstream.json | jq . && test "$(jq -r .repo upstreams/omc/upstream.json)" = "https://github.com/Yeachan-Heo/oh-my-claudecode.git"</automated>
  </verify>
  <done>upstreams/omc/upstream.json exists with valid JSON matching the upstream schema, repo URL is correct, and `ls upstreams/` shows gsd, omc, paul</done>
</task>

<task type="auto">
  <name>Task 2: Create cross-package pattern analysis document</name>
  <files>docs/cross-package-analysis.md</files>
  <action>
Create `docs/` directory and `docs/cross-package-analysis.md` -- the living analysis document per CONTEXT.md decisions.

Structure the document with these sections:

**Header:**
- Title: "Cross-Package Pattern Analysis"
- Last reviewed: 2026-03-07
- Upstreams analyzed: GSD (repo URL), PAUL (repo URL), OMC (SHA 96a5d372, v4.7.3)

**Section 1: Shared Patterns** -- Patterns appearing in 2+ upstreams. For each pattern, include:
- Side-by-side comparison table: Upstream | Implementation | Strength | Weakness
- Explicit recommendation (which to use or how to synthesize)
- Reference doc link (if a best-of-breed doc will be created in Plan 02)

Use the 5 patterns from the RESEARCH.md cross-package analysis:
1. Task Verification (GSD verify-work + PAUL protocol + OMC UltraQA cycling)
2. Context/State Management (GSD STATE.md + PAUL guidelines + OMC tiered notepad)
3. Planning Quality Gates (GSD plan-check + PAUL structured fields + OMC Ralplan consensus)
4. Model/Agent Routing (GSD model_profile + OMC agent tiers)
5. Structured Research (GSD research-phase + OMC SciOMC decomposition)

**Section 2: OMC Feature Inventory** -- The full 37-skill categorization from RESEARCH.md, organized in three sub-tables:
- Cherry-Picked: Feature | What We Took | Reference Doc (link to Plan 02 deliverables)
- Rejected: Feature | Reason | GSD/PAI Equivalent (document WHY for every rejection -- same pattern as PAUL rejection table in CLAUDE.md)
- Deferred: Feature | Why Deferred | Re-evaluate When

Use the RESEARCH.md OMC Skill Inventory as the source -- all 37 skills categorized there. The cherry-pick candidates are: ultrawork (model routing), ultraqa (verification cycling), ralplan (structured deliberation), deep-interview (ambiguity scoring), code-review (severity rating), sciomc (structured research), learner (skill extraction), note (tiered persistence).

**Section 3: Cherry-Pick Summary** -- Table mapping cherry-picked patterns to reference doc names and priority (HIGH/MEDIUM/LOW). This drives Plan 02's deliverables.

Add a note at the bottom: "This is a living document. When /skippy:update detects significant upstream changes (>10 commits or changes in cherry-picked areas), it will suggest re-reviewing this analysis."

Do NOT include PAI-specific OMC features (homekit, korean fiction, etc.) in the shared patterns section -- only in the Rejected inventory with appropriate notes.
  </action>
  <verify>
    <automated>test -f docs/cross-package-analysis.md && grep -c "Recommendation" docs/cross-package-analysis.md && grep -c "Reject" docs/cross-package-analysis.md</automated>
  </verify>
  <done>docs/cross-package-analysis.md exists with: (1) 5 shared pattern comparison tables each with explicit recommendations, (2) full 37-skill OMC inventory categorized as cherry-pick/reject/defer with reasons, (3) cherry-pick summary table linking to planned reference doc names</done>
</task>

</tasks>

<verification>
- `ls upstreams/` shows three directories: gsd, omc, paul
- `jq .name upstreams/omc/upstream.json` returns "omc"
- `docs/cross-package-analysis.md` exists and contains comparison tables with "Recommendation" entries
- Every rejected OMC skill has a documented reason
- Cherry-pick summary table lists at least 3 HIGH priority patterns
</verification>

<success_criteria>
- OMC is a registered upstream with valid upstream.json
- Cross-package analysis covers all 5 shared patterns with side-by-side comparisons
- All 37 OMC skills categorized with documented rationale
- Analysis document is structured to drive Plan 02's reference doc creation
</success_criteria>

<output>
After completion, create `.planning/phases/08-upstream-analysis/08-01-SUMMARY.md`
</output>
