---
phase: 15-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/deploy-service/config.env.example
  - skills/deploy-service/scripts/find-next-ip.sh
  - skills/deploy-service/scripts/install-base-stack.sh
  - skills/deploy-service/references/deploy-workflow.md
  - skills/deploy-service/references/nginx-proxy.conf
  - skills/deploy-service/SKILL.md
  - .gitignore
autonomous: true
requirements: [HARD-01, HARD-03]

must_haves:
  truths:
    - "No <your-*> placeholders remain in any deploy-service file"
    - "config.env.example exists with all 10 DEPLOY_* variables documented"
    - "config.env is gitignored so real values are never committed"
    - "Scripts fail with clear error when config.env is missing or incomplete"
  artifacts:
    - path: "skills/deploy-service/config.env.example"
      provides: "Configuration template with all 10 variables"
      contains: "DEPLOY_SERVER_IP"
    - path: "skills/deploy-service/scripts/find-next-ip.sh"
      provides: "IP scanner sourcing config.env"
      contains: "source.*config.env"
    - path: "skills/deploy-service/scripts/install-base-stack.sh"
      provides: "Base stack installer sourcing config.env"
      contains: "source.*config.env"
  key_links:
    - from: "skills/deploy-service/scripts/find-next-ip.sh"
      to: "skills/deploy-service/config.env"
      via: "source with :? validation"
      pattern: "source.*config\\.env"
    - from: "skills/deploy-service/scripts/install-base-stack.sh"
      to: "skills/deploy-service/config.env"
      via: "source with :? validation"
      pattern: "source.*config\\.env"
---

<objective>
Replace all hardcoded `<your-*>` angle-bracket placeholders across deploy-service with a shell-sourceable `config.env` mechanism.

Purpose: Users configure their infrastructure values once in config.env; all deploy-service scripts and reference docs read from it. No more manual find-and-replace across 5 files.
Output: config.env.example (committed), config.env (gitignored), all 5 deploy-service files updated to use DEPLOY_* variables.
</objective>

<execution_context>
@/Users/rico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hardening/15-RESEARCH.md

@skills/deploy-service/scripts/find-next-ip.sh
@skills/deploy-service/scripts/install-base-stack.sh
@skills/deploy-service/references/deploy-workflow.md
@skills/deploy-service/references/nginx-proxy.conf
@skills/deploy-service/SKILL.md
@.gitignore

<interfaces>
<!-- Placeholder-to-variable mapping (from RESEARCH.md inventory) -->

| Placeholder | Config Variable | Files |
|-------------|----------------|-------|
| `<your-server-ip>` | `DEPLOY_SERVER_IP` | SKILL.md, deploy-workflow.md |
| `<your-domain>` | `DEPLOY_DOMAIN` | SKILL.md, deploy-workflow.md, nginx-proxy.conf |
| `<your-proxy-host>` | `DEPLOY_PROXY_VMID` | SKILL.md |
| `<your-proxy-vmid>` | `DEPLOY_PROXY_VMID` | deploy-workflow.md |
| `<your-proxy-ip>` | `DEPLOY_PROXY_IP` | deploy-workflow.md |
| `<your-network-1>` | `DEPLOY_NET1` | find-next-ip.sh, deploy-workflow.md |
| `<your-network-2>` | `DEPLOY_NET2` | find-next-ip.sh, deploy-workflow.md |
| `<your-gateway>` | `DEPLOY_GATEWAY` | deploy-workflow.md |
| `<your-dns-servers>` | `DEPLOY_DNS_VMIDS` | SKILL.md, deploy-workflow.md |
| `<your-vaultwarden-url>` | `DEPLOY_VAULTWARDEN_URL` | install-base-stack.sh |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.env.example and gitignore config.env</name>
  <files>skills/deploy-service/config.env.example, .gitignore</files>
  <action>
Create `skills/deploy-service/config.env.example` with all 10 DEPLOY_* variables. Each variable gets a comment explaining what it is and an example value format. Variables should have empty string defaults (e.g., `DEPLOY_SERVER_IP=""`).

Variable list (all required unless noted):
- `DEPLOY_SERVER_IP` -- Proxmox host IP (e.g., 10.0.1.1)
- `DEPLOY_DOMAIN` -- Wildcard SSL domain (e.g., example.com)
- `DEPLOY_PROXY_VMID` -- VMID of nginx proxy container (e.g., 100)
- `DEPLOY_PROXY_IP` -- IP of proxy container for DNS entries (e.g., 10.0.2.100)
- `DEPLOY_NET1` -- First network prefix, first 3 octets (e.g., 10.0.1)
- `DEPLOY_NET2` -- Second network prefix, first 3 octets (e.g., 10.0.2)
- `DEPLOY_GATEWAY` -- Default gateway IP (e.g., 10.0.1.1)
- `DEPLOY_DNS_VMIDS` -- Space-separated DNS server VMIDs (e.g., "101 102")
- `DEPLOY_VAULTWARDEN_URL` -- Vaultwarden instance URL (e.g., vault.example.com)

Header comment: "deploy-service configuration / Copy to config.env and fill in your values. / config.env is gitignored -- never commit real values."

Add `skills/deploy-service/config.env` to `.gitignore` under a new "# Deploy-service config" section. Place it after the existing "Private content safety net" section. Do NOT remove or reorder any existing .gitignore patterns.
  </action>
  <verify>
    <automated>test -f skills/deploy-service/config.env.example && grep -c "DEPLOY_" skills/deploy-service/config.env.example | grep -q "10" && git check-ignore skills/deploy-service/config.env</automated>
  </verify>
  <done>config.env.example exists with all 10 DEPLOY_* variables documented. config.env path is gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholders with config.env sourcing in all deploy-service files</name>
  <files>skills/deploy-service/scripts/find-next-ip.sh, skills/deploy-service/scripts/install-base-stack.sh, skills/deploy-service/references/deploy-workflow.md, skills/deploy-service/references/nginx-proxy.conf, skills/deploy-service/SKILL.md</files>
  <action>
**Scripts (find-next-ip.sh, install-base-stack.sh):** Add config.env sourcing block after `set -euo pipefail`. Pattern:

```bash
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
CONFIG="$SCRIPT_DIR/../config.env"

if [[ ! -f "$CONFIG" ]]; then
    echo "ERROR: config.env not found at $CONFIG" >&2
    echo "  Copy config.env.example to config.env and fill in your values." >&2
    exit 1
fi

# shellcheck source=/dev/null
source "$CONFIG"
```

Then validate required variables with `:?` parameter expansion, then replace inline placeholders with variable references.

For **find-next-ip.sh**: Remove the `NET1="<your-network-1>"` / `NET2="<your-network-2>"` inline assignments. After sourcing, validate: `: "${DEPLOY_NET1:?DEPLOY_NET1 not set in config.env}"` and same for DEPLOY_NET2. Use `${DEPLOY_NET1}` and `${DEPLOY_NET2}` in the ping commands.

For **install-base-stack.sh**: After sourcing, validate DEPLOY_VAULTWARDEN_URL. Replace `'https://<your-vaultwarden-url>'` with `"https://${DEPLOY_VAULTWARDEN_URL}"` (double quotes for variable expansion).

**Reference docs (deploy-workflow.md):** This is an AI-consumed reference doc, not a directly executed script. Add a setup note at the top (after the `# Deploy Service Workflow` heading): "**Prerequisites:** Source `config.env` before executing these commands. See `config.env.example` for setup." Replace ALL `<your-*>` placeholders with `${DEPLOY_*}` variable references per the mapping table. The code blocks become executable after config is sourced.

**nginx-proxy.conf:** This is a template file that uses `{{mustache}}` vars for per-deployment values AND `<your-domain>` for environment config. Replace only the `<your-domain>` placeholders with `${DEPLOY_DOMAIN}`. Keep `{{SERVICE_NAME}}`, `{{BACKEND_IP}}`, `{{PORT}}` as-is -- those are per-deployment substitutions handled by sed in the workflow.

**SKILL.md:** In the "Configuration Required" section, replace the bullet list of `<your-*>` placeholders with instructions to copy config.env.example to config.env. In the Parameters section, replace `<your-domain>` with `${DEPLOY_DOMAIN}` in the example. In the "What You Get" section, replace `<your-domain>` similarly. Remove the `<your-proxy-host>` and `<your-dns-servers>` placeholder references -- they now live in config.env. Update the "PAI enhancements" note at the bottom to say "In PAI installations, config.env is pre-populated with actual infrastructure values."
  </action>
  <verify>
    <automated>grep -r '<your-' skills/deploy-service/ | grep -v config.env.example; test $? -eq 1</automated>
  </verify>
  <done>Zero `<your-*>` placeholders remain in any deploy-service file (except config.env.example which documents them as example formats). All scripts source config.env with `:?` validation. Reference docs use `${DEPLOY_*}` variable references.</done>
</task>

</tasks>

<verification>
1. `grep -r '<your-' skills/deploy-service/ --include='*.sh' --include='*.md' --include='*.conf' | grep -v config.env.example` returns zero matches
2. `test -f skills/deploy-service/config.env.example` succeeds
3. `git check-ignore skills/deploy-service/config.env` returns 0
4. `grep -c 'DEPLOY_' skills/deploy-service/config.env.example` returns 10
5. `grep -c 'source.*config.env' skills/deploy-service/scripts/find-next-ip.sh` returns 1
6. `grep -c 'source.*config.env' skills/deploy-service/scripts/install-base-stack.sh` returns 1
</verification>

<success_criteria>
- config.env.example committed with all 10 DEPLOY_* variables and documentation comments
- config.env gitignored
- find-next-ip.sh and install-base-stack.sh source config.env with missing-file error and :? validation
- deploy-workflow.md uses ${DEPLOY_*} variables with setup note
- nginx-proxy.conf uses ${DEPLOY_DOMAIN} ({{mustache}} vars unchanged)
- SKILL.md references config.env setup instead of listing raw placeholders
- Zero grep matches for `<your-` across deploy-service (excluding config.env.example)
</success_criteria>

<output>
After completion, create `.planning/phases/15-hardening/15-01-SUMMARY.md`
</output>
