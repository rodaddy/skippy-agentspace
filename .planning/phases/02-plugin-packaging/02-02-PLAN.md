---
phase: 02-plugin-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/install.sh
autonomous: true
requirements:
  - STRU-03

must_haves:
  truths:
    - "install.sh detects ~/.claude/skills/ (modern) vs ~/.claude/commands/ (legacy) automatically"
    - "install.sh supports --target=skills|commands|auto override flag"
    - "Modern install symlinks entire skill directory to ~/.claude/skills/<name>/"
    - "Legacy install symlinks commands/ subdirectory to ~/.claude/commands/<name>"
    - "install.sh uses #!/usr/bin/env bash shebang"
    - "install.sh handles re-install (replaces existing symlink)"
  artifacts:
    - path: "tools/install.sh"
      provides: "Dual-target skill installer"
      contains: "skills"
---

<objective>
Rewrite install.sh to support both modern (`~/.claude/skills/`) and legacy (`~/.claude/commands/`) install targets with auto-detection.

Current install.sh only targets `~/.claude/commands/`. The new version auto-detects the best target and supports explicit override.

Output: tools/install.sh with dual-target support, auto-detection, and clean re-install handling.
</objective>

<context>
@tools/install.sh
@.planning/phases/02-plugin-packaging/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite install.sh with dual-target support</name>
  <files>tools/install.sh</files>
  <action>
Read current tools/install.sh, then rewrite with dual-target support.

Requirements:
- Auto-detect: if `~/.claude/skills/` exists, use modern target. Otherwise legacy.
- Flag override: `--target=skills`, `--target=commands`, `--target=auto` (default)
- Modern target: `ln -s <repo>/skills/skippy-dev ~/.claude/skills/skippy-dev`
- Legacy target: `ln -s <repo>/skills/skippy-dev/commands ~/.claude/commands/skippy`
- Handle re-install: if symlink exists, remove and recreate
- Warn if plugin-installed version detected in `~/.claude/plugins/cache/`
- Use `#!/usr/bin/env bash` shebang
- Use `set -euo pipefail`
- Print clear success messages showing what was linked where
- Support `--all` flag to install all skills found in skills/ directory
- Support named skill argument: `install.sh skippy-dev`
  </action>
  <verify>
    <automated>head -1 tools/install.sh | grep -q '#!/usr/bin/env bash' && grep -q 'skills' tools/install.sh && grep -q 'commands' tools/install.sh && echo "PASS: dual-target support present" || echo "FAIL"</automated>
  </verify>
  <done>install.sh supports auto-detection of modern vs legacy target, --target override flag, clean re-install, and plugin conflict warning.</done>
</task>

</tasks>
