---
phase: 02-plugin-packaging
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/uninstall.sh
  - INDEX.md
  - CLAUDE.md
autonomous: true
requirements:
  - STRU-03

must_haves:
  truths:
    - "uninstall.sh removes symlinks from both ~/.claude/skills/ and ~/.claude/commands/"
    - "uninstall.sh uses #!/usr/bin/env bash shebang"
    - "INDEX.md reflects current skill metadata accurately"
    - "CLAUDE.md file tree shows .claude-plugin/ directory"
  artifacts:
    - path: "tools/uninstall.sh"
      provides: "Dual-target skill uninstaller"
      contains: "skills"
    - path: "INDEX.md"
      provides: "Updated skill registry"
      contains: "skippy-dev"
---

<objective>
Rewrite uninstall.sh for dual-target cleanup, update INDEX.md to reflect plugin packaging, and update CLAUDE.md file tree to include .claude-plugin/.

Output: tools/uninstall.sh with dual-target removal, updated INDEX.md and CLAUDE.md.
</objective>

<context>
@tools/uninstall.sh
@INDEX.md
@CLAUDE.md
@.planning/phases/02-plugin-packaging/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite uninstall.sh with dual-target support</name>
  <files>tools/uninstall.sh</files>
  <action>
Read current tools/uninstall.sh, then rewrite with dual-target support.

Requirements:
- Check both `~/.claude/skills/<name>` and `~/.claude/commands/<name>` for symlinks
- Remove from whichever locations have symlinks
- Report what was removed
- Use `#!/usr/bin/env bash` and `set -euo pipefail`
- Support named skill argument or `--all` flag
- Warn (don't error) if nothing found to uninstall
  </action>
  <verify>
    <automated>head -1 tools/uninstall.sh | grep -q '#!/usr/bin/env bash' && grep -q 'skills' tools/uninstall.sh && grep -q 'commands' tools/uninstall.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>uninstall.sh removes symlinks from both modern and legacy targets with clear reporting.</done>
</task>

<task type="auto">
  <name>Task 2: Update INDEX.md and CLAUDE.md</name>
  <files>INDEX.md, CLAUDE.md</files>
  <action>
Update INDEX.md to reflect plugin packaging status -- add note about marketplace.json and plugin install method.

Update CLAUDE.md "What's Already Built" file tree to include:
```
.claude-plugin/
  marketplace.json      # Plugin marketplace definition (strict: false)
```

Also update the Phase 2 status in CLAUDE.md status table if present.
  </action>
  <verify>
    <automated>grep -q 'marketplace' INDEX.md && grep -q 'claude-plugin' CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>INDEX.md reflects plugin packaging. CLAUDE.md file tree includes .claude-plugin/.</done>
</task>

</tasks>
