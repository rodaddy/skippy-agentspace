---
phase: 04-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/skippy/references/gsd-dependency-map.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "GSD dependency map document exists at skills/skippy/references/gsd-dependency-map.md"
    - "Document lists every .planning/ file path the skill reads or depends on"
    - "Each integration point has a breakage risk annotation (what happens if GSD changes it)"
    - "Document covers PLAN.md structure (YAML frontmatter + XML tasks), SUMMARY.md structure, STATE.md, ROADMAP.md, REQUIREMENTS.md"
  artifacts:
    - path: "skills/skippy/references/gsd-dependency-map.md"
      provides: "GSD integration point documentation"
      contains: "breakage"
---

<objective>
Create a GSD dependency map documenting every .planning/ integration point used by skippy commands.

The reconcile command reads PLAN.md (YAML + XML), SUMMARY.md, STATE.md, ROADMAP.md. The update and cleanup commands don't directly depend on GSD structures but live in the same skill. Document what each command reads, the exact format it expects, and what breaks if GSD changes upstream.

Output: skills/skippy/references/gsd-dependency-map.md with every integration point annotated with breakage risk.
</objective>

<context>
@skills/skippy/commands/reconcile.md
@skills/skippy/commands/update.md
@skills/skippy/commands/cleanup.md
@skills/skippy/references/reconciliation.md
@.planning/phases/01-spec-compliance/01-01-PLAN.md
@.planning/phases/01-spec-compliance/01-01-SUMMARY.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GSD dependency map</name>
  <files>skills/skippy/references/gsd-dependency-map.md</files>
  <action>
Read all the context files to understand what GSD structures skippy depends on. Then create a comprehensive dependency map at `skills/skippy/references/gsd-dependency-map.md`.

The document should have:

1. **Overview** -- what this document is and why it matters
2. **Integration Points Table** -- for each file type:
   - File path pattern (e.g., `.planning/phases/NN-*/NN-*-PLAN.md`)
   - What reads it (which skippy command)
   - Fields/sections depended on (be specific: YAML keys, XML elements, markdown headers)
   - Breakage risk (HIGH/MEDIUM/LOW) with description of what breaks if GSD changes that format

3. **PLAN.md Structure** -- exact format reconcile expects (YAML frontmatter fields, XML task blocks)
4. **SUMMARY.md Structure** -- exact format reconcile compares against
5. **STATE.md Structure** -- frontmatter fields used for phase detection
6. **ROADMAP.md Structure** -- `[x]` markers, phase headers, progress table

Each section should have a "If GSD changes this..." annotation explaining the impact.

Keep it practical -- this is a reference doc for maintaining compatibility, not an academic paper.
  </action>
  <verify>
    <automated>test -f skills/skippy/references/gsd-dependency-map.md && grep -q 'breakage\|Breakage\|BREAKAGE' skills/skippy/references/gsd-dependency-map.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>GSD dependency map exists with integration points, format specifications, and breakage risk annotations.</done>
</task>

</tasks>
