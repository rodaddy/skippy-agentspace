---
phase: 03-command-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/skippy-dev/scripts/skippy-cleanup.sh
autonomous: true
requirements:
  - CMD-03

must_haves:
  truths:
    - "Default quarantine path uses ~/.cache/skippy-quarantine, not $TMPDIR"
    - "Quarantine mode moves files and reports space freed"
    - "Nuke mode deletes permanently and recreates empty dirs"
    - "Script exits cleanly with informative messages when no targets exist"
    - "Script uses #!/usr/bin/env bash shebang"
  artifacts:
    - path: "skills/skippy-dev/scripts/skippy-cleanup.sh"
      provides: "Validated cleanup script"
      contains: ".cache/skippy-quarantine"
---

<objective>
Fix the TMPDIR quarantine bug and validate both cleanup modes work correctly.

The default quarantine path uses $TMPDIR which on macOS is a session-specific path that gets cleaned on reboot. Change to ~/.cache/skippy-quarantine for persistence.

Output: Cleanup script with persistent quarantine default and validated edge case handling.
</objective>

<context>
@skills/skippy-dev/scripts/skippy-cleanup.sh
@skills/skippy-dev/commands/cleanup.md
@.planning/phases/03-command-validation/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TMPDIR quarantine bug and validate cleanup</name>
  <files>skills/skippy-dev/scripts/skippy-cleanup.sh</files>
  <action>
Read current skippy-cleanup.sh and fix:

**Bug - TMPDIR default (MEDIUM):**
Change line 10 from:
```bash
QUARANTINE_BASE="${SKIPPY_QUARANTINE_DIR:-${TMPDIR:-/tmp}/skippy-cleanup}"
```
To:
```bash
QUARANTINE_BASE="${SKIPPY_QUARANTINE_DIR:-${HOME}/.cache/skippy-quarantine}"
```

Also review and fix any other edge cases:
- Ensure script handles missing target directories gracefully (SKIP messages)
- Ensure empty directories are properly recreated after cleanup
- Ensure space reporting is accurate (du -sk)
- Verify the script handles the case where quarantine dir doesn't exist yet (mkdir -p)
  </action>
  <verify>
    <automated>grep -q '\.cache/skippy-quarantine' skills/skippy-dev/scripts/skippy-cleanup.sh && ! grep -q 'TMPDIR' skills/skippy-dev/scripts/skippy-cleanup.sh && echo "PASS: persistent quarantine, no TMPDIR" || echo "FAIL"</automated>
  </verify>
  <done>Default quarantine uses ~/.cache/skippy-quarantine. Script handles missing dirs, empty dirs, and reports space accurately.</done>
</task>

</tasks>
