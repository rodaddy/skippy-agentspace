---
phase: 03-command-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/skippy/commands/reconcile.md
autonomous: true
requirements:
  - CMD-01

must_haves:
  truths:
    - "reconcile.md instructs agent to find most recently completed phase via ROADMAP.md [x] markers"
    - "reconcile.md instructs agent to glob for ALL plans in a phase (NN-*-PLAN.md), not assume single plan"
    - "reconcile.md instructs agent to extract tasks from <task> XML blocks"
    - "reconcile.md instructs agent to check for unplanned file changes via git commit history"
    - "reconcile.md instructs agent to always save RECONCILIATION.md, not just optionally"
    - "reconcile.md supports user-specified phase number argument"
  artifacts:
    - path: "skills/skippy/commands/reconcile.md"
      provides: "Enhanced reconcile command prompt"
      contains: "PLAN.md"
---

<objective>
Enhance the reconcile command to work correctly against real GSD phases with multi-plan support, proper phase detection, and XML task parsing guidance.

The command is a Claude Code prompt, not a script. The fix is improving the instructions so the agent correctly parses GSD artifacts. This project's own Phase 1 (1 plan, 1 deviation) and Phase 2 (3 plans, 0 deviations) serve as real validation fixtures.

Output: Enhanced reconcile.md that handles multi-plan phases, finds latest completed phase, extracts XML tasks, and always saves output.
</objective>

<context>
@skills/skippy/commands/reconcile.md
@skills/skippy/references/reconciliation.md
@skills/skippy/references/state-consistency.md
@.planning/phases/03-command-validation/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance reconcile.md with multi-plan and phase detection</name>
  <files>skills/skippy/commands/reconcile.md</files>
  <action>
Read the current reconcile.md, then rewrite the process section to address these 5 gaps:

1. **Phase detection:** Add clear instructions to find the most recently completed phase:
   - Read ROADMAP.md, find last phase marked `[x]`
   - If user specifies a phase number, use that instead
   - Verify against STATE.md frontmatter

2. **Multi-plan support:** Change "Read the phase's PLAN.md" to:
   - Glob for ALL plans: `phases/<NN>-*/<NN>-*-PLAN.md`
   - Process EACH plan with its matching SUMMARY.md
   - Report per-plan and aggregate results

3. **XML task extraction:** Add guidance that tasks are in `<task>` XML blocks with `<name>`, `<files>`, `<action>`, `<verify>`, `<done>` children.

4. **Git diff for unplanned changes:** Add step to use SUMMARY.md commit hashes to check for files changed that weren't in the plan's `files_modified` list.

5. **Always save output:** Change "optionally saved" to always save to `.planning/phases/<NN>-*/RECONCILIATION.md`.

Keep the overall structure and @ references intact. Don't change the frontmatter references.
  </action>
  <verify>
    <automated>grep -c 'PLAN.md' skills/skippy/commands/reconcile.md | xargs -I{} test {} -ge 2 && grep -q 'RECONCILIATION.md' skills/skippy/commands/reconcile.md && grep -q 'ROADMAP' skills/skippy/commands/reconcile.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>reconcile.md handles multi-plan phases, detects latest completed phase from ROADMAP.md, guides XML task extraction, checks unplanned git changes, and always saves RECONCILIATION.md.</done>
</task>

</tasks>
