---
phase: 03-command-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/skippy/scripts/skippy-update.sh
  - skills/skippy/commands/update.md
autonomous: true
requirements:
  - CMD-02

must_haves:
  truths:
    - "No 'source' command used to parse .versions file (security fix)"
    - "Full 40-character SHA hashes stored in .versions (no --short flag)"
    - "update.md references ~/.cache/skippy-upstream or SKIPPY_CACHE_DIR, not /tmp/"
    - "Network failures for one repo don't prevent reporting results for the other"
    - "Script uses #!/usr/bin/env bash shebang"
  artifacts:
    - path: "skills/skippy/scripts/skippy-update.sh"
      provides: "Hardened update script"
      contains: "while IFS"
    - path: "skills/skippy/commands/update.md"
      provides: "Update command with correct paths"
---

<objective>
Fix 3 concrete bugs in the update script and 1 stale path in update.md:

1. Replace `source "$VERSIONS_FILE"` with safe `while IFS='=' read` parsing (security)
2. Remove `--short` flag from `git rev-parse` to store full 40-char SHA hashes (stability)
3. Fix stale `/tmp/skippy-upstream/` reference in update.md line 22 (correctness)
4. Add per-repo error handling so PAUL clone failure doesn't prevent GSD reporting (resilience)

Output: Hardened update script and corrected command file.
</objective>

<context>
@skills/skippy/scripts/skippy-update.sh
@skills/skippy/commands/update.md
@skills/skippy/.versions
@.planning/phases/03-command-validation/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix update script bugs</name>
  <files>skills/skippy/scripts/skippy-update.sh</files>
  <action>
Read current skippy-update.sh and fix these 3 bugs:

**Bug 1 - source command (CRITICAL):**
Replace `source "$VERSIONS_FILE"` with safe key=value parsing:
```bash
gsd_hash="none"
paul_hash="none"
last_check="never"
if [[ -f "$VERSIONS_FILE" ]]; then
    while IFS='=' read -r key value; do
        case "$key" in
            gsd_hash)   gsd_hash="$value" ;;
            paul_hash)  paul_hash="$value" ;;
            last_check) last_check="$value" ;;
        esac
    done < "$VERSIONS_FILE"
fi
```

**Bug 2 - short SHA (MEDIUM):**
Change `git rev-parse --short HEAD` to `git rev-parse HEAD` (2 occurrences).
Display can truncate: `echo "  Current: ${GSD_CURRENT:0:10}"`

**Bug 3 - error handling (MEDIUM):**
Wrap clone/fetch operations in a function with explicit error handling so one repo's failure doesn't prevent the other from being checked. Remove reliance on `set -e` for network operations.
  </action>
  <verify>
    <automated>grep -c '^source ' skills/skippy/scripts/skippy-update.sh | xargs -I{} test {} -eq 0 && ! grep -q 'rev-parse --short' skills/skippy/scripts/skippy-update.sh && echo "PASS: no source, no --short" || echo "FAIL"</automated>
  </verify>
  <done>No source command for .versions parsing. Full SHA hashes stored. Independent error handling per repo.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale path in update.md</name>
  <files>skills/skippy/commands/update.md</files>
  <action>
Fix the stale `/tmp/skippy-upstream/` reference in update.md. Replace with reference to `~/.cache/skippy-upstream` or `$SKIPPY_CACHE_DIR`.
  </action>
  <verify>
    <automated>! grep -q '/tmp/skippy' skills/skippy/commands/update.md && echo "PASS: no /tmp/ reference" || echo "FAIL"</automated>
  </verify>
  <done>update.md references correct cache directory, no /tmp/ paths.</done>
</task>

</tasks>
