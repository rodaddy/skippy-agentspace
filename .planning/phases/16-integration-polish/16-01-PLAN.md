---
phase: 16-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - FOUND-03
must_haves:
  truths:
    - "CONTRIBUTING.md exists with sections for adding skills, running tests, and submitting changes"
    - "CLAUDE.md reflects standalone identity -- Skippy IS the framework, not a GSD parasite"
    - "README.md has correct reference doc count (18, not 15) and accurate upstream table"
    - "README.md includes bats test instructions alongside integration-test.sh"
    - "CLAUDE.md project status table includes Phases 12-16"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Contributor guide for skill addition, testing, and submissions"
      contains: "Adding a Skill"
    - path: "CLAUDE.md"
      provides: "Updated project context with standalone framing"
      contains: "standalone"
    - path: "README.md"
      provides: "Public-facing docs with accurate counts and framing"
      contains: "18 reference docs"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "CONVENTIONS.md"
      via: "reference link"
      pattern: "CONVENTIONS\\.md"
    - from: "README.md"
      to: "skills/skippy-dev/references/"
      via: "reference doc listing"
      pattern: "Reference Docs.*18"
---

<objective>
Create CONTRIBUTING.md and update CLAUDE.md + README.md to reflect standalone identity and accurate v1.2 state.

Purpose: Satisfy FOUND-03 (contributor documentation) and update public-facing docs to reflect that skippy is now a standalone framework, not a GSD parasite. All v1.2 features (Phases 11-15) are complete -- these docs need to catch up.

Output: CONTRIBUTING.md (new), CLAUDE.md (updated), README.md (updated)
</objective>

<execution_context>
@/Users/rico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-integration-polish/16-RESEARCH.md

@CONVENTIONS.md
@CLAUDE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <action>
Create CONTRIBUTING.md as a routing document -- under 100 lines. Do NOT duplicate content from CONVENTIONS.md, SETUP.md, or INSTALL.md. Reference them instead.

Required sections:

1. **Adding a Skill** -- Directory structure (`skills/<name>/`), SKILL.md frontmatter requirements (name, description, metadata.version/author/source/category), slim SKILL.md convention (under 150 lines, detail in `references/` subdirectory), integration checklist: add to `.claude-plugin/marketplace.json`, run `bash tools/index-sync.sh --generate` to update INDEX.md, no cross-skill imports (portability constraint).

2. **Running Tests** -- Three commands with descriptions:
   - `./tests/bats/bin/bats tests/` -- unit tests (37 cases, sandboxed HOME, never touches real ~/.claude/)
   - `bash tools/verify.sh` -- health check (24+ checks across 5 categories)
   - `bash tools/integration-test.sh` -- full integration suite (36 tests, clones upstreams)
   - `bash tools/integration-test.sh --quick` -- skip upstream clones

3. **Submitting Changes** -- Branch naming: `feat/`, `fix/`, `wip/` prefixes (hook-enforced, never commit to main). Shell script conventions: `#!/usr/bin/env bash`, `set -euo pipefail`. Run tests before submitting. Reference CONVENTIONS.md for full coding conventions and content classification.

4. **Conventions** -- Brief note referencing CONVENTIONS.md. Mention: `skippy_` namespace for shared functions, `validate_skill_name()` for user input, no cross-skill imports.

End with a link to CONVENTIONS.md for full details.
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && grep -q "Adding a Skill" CONTRIBUTING.md && grep -q "Running Tests" CONTRIBUTING.md && grep -q "Submitting Changes" CONTRIBUTING.md && grep -q "SKILL.md" CONTRIBUTING.md && grep -q "bats" CONTRIBUTING.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CONTRIBUTING.md exists with all 4 required sections, under 100 lines, references CONVENTIONS.md instead of duplicating content</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md and README.md for standalone identity</name>
  <files>CLAUDE.md, README.md</files>
  <action>
**CLAUDE.md changes:**

1. Line 3 (blockquote): Change "Cherry-picks the best workflow ideas from GSD and PAUL" to "Standalone Claude Code skill framework with patterns adapted from GSD, PAUL, and OMC"

2. Line 14 ("Not a fork" paragraph): Change "Not a fork. All upstreams ride unchanged -- enhancements are additive reference docs and utility commands." to "Standalone skill framework. Upstream repos (GSD, PAUL, OMC) are historical sources of adapted patterns, not runtime dependencies."

3. Line 54 (skippy-dev tree comment): Change "15 reference docs" to "18 reference docs"

4. Line 118 ("No GSD dependency" constraint): Change to "**Standalone**: No runtime dependency on GSD, PAUL, or OMC. Historical source attribution only."

5. Project Status table (after line 134): Add rows for Phases 12-16:
   - 12. Testing | bats-core test suite, CI workflow | Complete
   - 13. GSD Absorption | Standalone reference docs, state parser | Complete
   - 14. Audit Swarm | /skippy:review multi-agent command | Complete
   - 15. Hardening | deploy-service config, version bump | Complete
   - 16. Integration & Polish | CONTRIBUTING.md, doc consistency | In Progress

6. Update the project status summary line (line 123) from "v1.2 Phase 11 complete -- shared shell library and repo hygiene." to "v1.2 in progress -- 12 skills, 18 reference docs, standalone framework."

**README.md changes:**

1. Line 1 description: Change "Cherry-picks the best workflow ideas from" to "Standalone Claude Code skill framework with patterns adapted from"

2. Line 7: Change "15 reference docs" to "18 reference docs"

3. Line 51: Change "Cherry-Picked Reference Docs (15)" to "Reference Docs (18)"

4. Lines 55-63 (reference doc grouping): Replace with accurate grouping:
   - **From PAUL (5)** -- planning discipline: context brackets, reconciliation, plan structure (task format), plan boundaries, state consistency
   - **From OMC (4)** -- execution readiness: ambiguity scoring, compaction resilience, parallel file ownership, pre-execution gate
   - **From GSD (5)** -- execution patterns: phased execution, state tracking, checkpoints, plan structure (plan format), verification loops
   - **Cross-source (3)** -- synthesized patterns: model routing, structured deliberation, session persistence
   - **Original (1)** -- audit swarm (from v1.1 audit process)

5. Lines 68-72 (upstream table): Replace with:
   | Upstream | What We Take | Patterns Adapted |
   |----------|-------------|-----------------|
   | [GSD](https://github.com/gsd-build/get-shit-done) | Historical source of phased execution patterns | 5 |
   | [PAUL](https://github.com/ChristopherKahler/paul) | 5 planning discipline ideas | 5 |
   | [OMC](https://github.com/anthropics/oh-my-claudecode) | 4 execution readiness patterns | 4 |

6. Lines 106-110 (Testing section): Add bats test command before integration-test.sh:
   ```bash
   # Unit tests (bats-core, sandboxed HOME)
   ./tests/bats/bin/bats tests/

   # Health check
   bash tools/verify.sh

   # Full integration suite
   bash tools/integration-test.sh
   bash tools/integration-test.sh --quick    # Skip upstream clones
   bash tools/integration-test.sh --verbose  # Show full output
   ```

7. Add `bump-version.sh` to the Tools table: `tools/bump-version.sh | Version bump across all 25 version locations`

IMPORTANT: Do NOT modify .planning/ files -- those are historical records and are export-ignored via .gitattributes.
  </action>
  <verify>
    <automated>grep -q "standalone" CLAUDE.md && grep -q "18 reference docs" README.md && grep -q "bats tests/" README.md && grep -q "Patterns Adapted" README.md && ! grep -q "Cherry-Picked Reference Docs (15)" README.md && ! grep -q "0 (dependency)" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CLAUDE.md uses standalone framing (no "cherry-picks" or "not a fork" language), includes Phases 12-16 in status table. README.md has correct reference doc count (18), accurate upstream table without "0 (dependency)", bats test instructions, and bump-version.sh in tools table.</done>
</task>

</tasks>

<verification>
- `grep -c "Adding a Skill\|Running Tests\|Submitting Changes" CONTRIBUTING.md` returns 3
- `grep -q "standalone" CLAUDE.md` succeeds
- `grep -q "18 reference docs" README.md` succeeds
- `grep -q "bats tests/" README.md` succeeds
- `! grep -q "15 reference docs" README.md` succeeds (old count removed)
- `! grep -q "0 (dependency)" README.md` succeeds (old GSD row removed)
- `wc -l CONTRIBUTING.md` returns under 100
</verification>

<success_criteria>
- CONTRIBUTING.md exists with required sections (FOUND-03 satisfied)
- CLAUDE.md and README.md reflect standalone identity
- All reference doc counts accurate (18)
- No stale GSD dependency framing in distributed docs
</success_criteria>

<output>
After completion, create `.planning/phases/16-integration-polish/16-01-SUMMARY.md`
</output>
