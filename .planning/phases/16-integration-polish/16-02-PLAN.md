---
phase: 16-integration-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - tools/verify.sh
autonomous: true
requirements:
  - FOUND-03
must_haves:
  truths:
    - "verify.sh checks all 6 commands including review"
    - "verify.sh passes with zero failures after all v1.2 changes"
    - "INDEX.md is consistent with actual skill state"
    - "All 37 bats tests pass"
  artifacts:
    - path: "tools/verify.sh"
      provides: "Health check script with review command verification"
      contains: "review"
  key_links:
    - from: "tools/verify.sh"
      to: "skills/skippy-dev/commands/review.md"
      via: "command existence check loop"
      pattern: "for cmd_name in.*review"
---

<objective>
Fix verify.sh to check the review command, regenerate INDEX.md, and run the full verification suite to confirm all v1.2 changes are consistent.

Purpose: Close the last verification gap (verify.sh only checks 5 of 6 commands) and run the final green-light pass confirming all Phase 16 documentation updates are consistent with the codebase.

Output: Fixed verify.sh, regenerated INDEX.md, clean verification pass
</objective>

<execution_context>
@/Users/rico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-integration-polish/16-RESEARCH.md
@.planning/phases/16-integration-polish/16-01-SUMMARY.md

@tools/verify.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix verify.sh command check and run final verification</name>
  <files>tools/verify.sh</files>
  <action>
1. **Fix verify.sh line 209**: Change the command check loop from:
   ```bash
   for cmd_name in reconcile update cleanup migrate upgrade; do
   ```
   to:
   ```bash
   for cmd_name in reconcile update cleanup migrate upgrade review; do
   ```
   This adds the `/skippy:review` command (created in Phase 14) to the health check.

2. **Regenerate INDEX.md**: Run `bash tools/index-sync.sh --generate` to ensure INDEX.md reflects all current skills and commands. Verify the output includes `/skippy:review`.

3. **Run full verification suite**:
   - `bash tools/verify.sh` -- must report zero failures
   - `./tests/bats/bin/bats tests/` -- all 37 tests must pass
   - `./tests/bats/bin/bats tests/verify.bats` -- verify.sh-specific tests must pass

4. **Verify Plan 01 changes haven't broken anything**:
   - `grep -q "review" tools/verify.sh` -- review command in check loop
   - `grep -q "Adding a Skill" CONTRIBUTING.md` -- CONTRIBUTING.md sections exist
   - `grep -q "18 reference docs" README.md` -- correct reference doc count
   - `grep -q "standalone" CLAUDE.md` -- standalone framing present

Do NOT modify any other files. This is a one-line fix to verify.sh plus validation.
  </action>
  <verify>
    <automated>grep -q "review" tools/verify.sh && bash tools/verify.sh 2>&1 | tail -5 && ./tests/bats/bin/bats tests/verify.bats && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>verify.sh checks all 6 commands (including review), verify.sh reports zero failures, all bats tests pass, INDEX.md is consistent</done>
</task>

</tasks>

<verification>
- `grep "for cmd_name in" tools/verify.sh` shows "review" in the list
- `bash tools/verify.sh` exits 0 with zero failures in summary
- `./tests/bats/bin/bats tests/` reports 37 tests, 0 failures
- `bash tools/index-sync.sh --check` reports no inconsistencies
- `test -f CONTRIBUTING.md` confirms CONTRIBUTING.md exists
</verification>

<success_criteria>
- verify.sh checks all 6 skippy-dev commands
- Full verification suite passes (zero failures)
- All bats tests pass
- INDEX.md consistent with actual skills/commands
- Phase 16 success criteria from ROADMAP all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/16-integration-polish/16-02-SUMMARY.md`
</output>
