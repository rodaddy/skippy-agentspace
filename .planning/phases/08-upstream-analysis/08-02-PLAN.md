---
phase: 08-upstream-analysis
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - skills/skippy/references/model-routing.md
  - skills/skippy/references/verification-loops.md
  - skills/skippy/references/session-persistence.md
  - skills/skippy/references/structured-deliberation.md
  - skills/skippy/references/skill-extraction.md
autonomous: true
requirements:
  - UPST-03

must_haves:
  truths:
    - "At least 3 best-of-breed reference docs exist that synthesize patterns from multiple upstreams"
    - "Each reference doc credits source upstreams inline and explains why this version is best"
    - "Reference docs follow the evolved format: Source Upstreams table, Why This Version, The Pattern, Integration Points, When to Apply"
    - "Reference docs contain actionable guidance, not just feature comparisons"
  artifacts:
    - path: "skills/skippy/references/model-routing.md"
      provides: "Model/agent tier selection guidance synthesized from OMC + GSD"
      min_lines: 40
    - path: "skills/skippy/references/verification-loops.md"
      provides: "Cycling verification pattern synthesized from OMC + PAUL + GSD"
      min_lines: 40
    - path: "skills/skippy/references/session-persistence.md"
      provides: "Tiered state persistence synthesized from OMC + GSD"
      min_lines: 40
  key_links:
    - from: "skills/skippy/references/model-routing.md"
      to: "docs/cross-package-analysis.md"
      via: "Implements recommendation from Model/Agent Routing pattern"
      pattern: "Source Upstreams"
    - from: "skills/skippy/references/verification-loops.md"
      to: "docs/cross-package-analysis.md"
      via: "Implements recommendation from Task Verification pattern"
      pattern: "Source Upstreams"
---

<objective>
Create best-of-breed reference docs that synthesize the strongest implementations of shared patterns across GSD, PAUL, and OMC upstreams.

Purpose: These reference docs are the tangible cherry-pick output -- actionable guidance that agents and developers can load on demand, distilling the best ideas from all three upstreams into single authoritative references.
Output: 3-5 best-of-breed reference docs in skills/skippy/references/
</objective>

<execution_context>
@/Users/rico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-upstream-analysis/08-CONTEXT.md
@.planning/phases/08-upstream-analysis/08-RESEARCH.md
@.planning/phases/08-upstream-analysis/08-01-SUMMARY.md

<!-- The cross-package analysis document drives which docs to create -->
@docs/cross-package-analysis.md

<interfaces>
<!-- Existing reference doc format (from PAUL cherry-picks) as the baseline to evolve -->
From skills/skippy/references/context-brackets.md (format example):
- ~2-5KB markdown
- Actionable guidance with concrete rules
- When to Apply section
- No OMC-specific tools or file paths

Evolved format per CONTEXT.md decisions adds:
- Source Upstreams table (Upstream | Implementation | Strength | Weakness)
- Why This Version section (explain synthesis rationale)
- Integration Points section (how it fits GSD workflow)
- Sources footer with upstream file references and last-reviewed date
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HIGH priority best-of-breed reference docs</name>
  <files>skills/skippy/references/model-routing.md, skills/skippy/references/verification-loops.md, skills/skippy/references/session-persistence.md</files>
  <action>
Create 3 reference docs using the evolved best-of-breed format. Each doc must follow this structure:

```
# [Pattern Name] -- Best-of-Breed Synthesis

[One-line description]. Synthesized from [upstream1] and [upstream2].

## Source Upstreams

| Upstream | Implementation | Strength | Weakness |
|----------|---------------|----------|----------|
| ... | ... | ... | ... |

## Why This Version

[Which parts came from where and why this synthesis is better than any single source]

## The Pattern

[Actual actionable content -- the meat of the doc]

## Integration Points

[How this fits into the skippy/GSD workflow, which phases it applies to]

## When to Apply

[Conditions where this pattern is relevant]

---
*Sources: [upstream1] [file], [upstream2] [file]*
*Last reviewed: 2026-03-07*
```

**model-routing.md** -- Synthesize from OMC's ultrawork (Haiku/Sonnet/Opus agent tiers by task complexity) + GSD's model_profile config (quality/speed/cost profiles). Extract the PRINCIPLE of matching model capability to task complexity. Include a tier decision table: task type -> recommended model tier -> rationale. Credit OMC for the tier concept, GSD for the profile structure. Do NOT reference OMC-specific tools like `state_write` or agent spawning APIs -- extract the decision heuristic only.

**verification-loops.md** -- Synthesize from OMC's UltraQA (cycling verification: test-diagnose-fix-repeat, max 5 iterations, same-error detection, early exit) + PAUL's verification protocol (explicit pass/fail criteria) + GSD's verify-work (single-pass phase verification). The key insight is CYCLING with bounded iterations. Include: when to single-pass vs cycle, iteration limits, same-error detection as exit signal, and how to structure pass/fail criteria. Credit each upstream for its contribution.

**session-persistence.md** -- Synthesize from OMC's note skill (Priority/Working/Manual tiers with auto-pruning) + GSD's STATE.md (git-tracked persistent state). Extract the TIERED PERSISTENCE concept: always-loaded context vs working memory vs manual notes. Map to GSD's existing artifacts (STATE.md = priority, SUMMARY.md = working, PROJECT.md = manual). Do NOT reference `.omc/notepad.md` or MCP tools -- adapt the concept for our context.

Each doc should be 40-80 lines, actionable, and usable by an agent without loading the full upstream source.
  </action>
  <verify>
    <automated>for f in model-routing verification-loops session-persistence; do test -f "skills/skippy/references/${f}.md" && grep -q "Source Upstreams" "skills/skippy/references/${f}.md" && grep -q "Why This Version" "skills/skippy/references/${f}.md" && echo "${f}: OK" || echo "${f}: MISSING"; done</automated>
  </verify>
  <done>3 reference docs exist with evolved format (Source Upstreams table, Why This Version, The Pattern, Integration Points, When to Apply sections), each 40-80 lines of actionable guidance crediting source upstreams</done>
</task>

<task type="auto">
  <name>Task 2: Create MEDIUM priority reference docs if analysis warrants</name>
  <files>skills/skippy/references/structured-deliberation.md, skills/skippy/references/skill-extraction.md</files>
  <action>
Review the cross-package analysis document (docs/cross-package-analysis.md) from Plan 01. If the analysis confirms these patterns are substantial enough for standalone docs, create them using the same evolved format.

**structured-deliberation.md** -- Synthesize from OMC's Ralplan (Planner+Architect+Critic consensus, bounded iterations, RALPLAN-DR structured output) + GSD's plan-check (single-reviewer plan validation). Extract the MULTI-PERSPECTIVE REVIEW concept: having distinct reviewer roles (planner, architect, critic) that challenge plans from different angles. Include: when to use single-reviewer vs multi-perspective, role definitions, iteration bounds, and structured decision record format. This is NOT about implementing OMC's multi-agent runtime -- it's about the HEURISTIC of thinking from multiple perspectives.

**skill-extraction.md** -- From OMC's learner skill (extract reusable skills from conversations with quality gates: non-Googleable, context-specific, actionable, hard-won). Extract the SKILL EXTRACTION METHODOLOGY: how to identify conversation moments worth preserving as reusable knowledge, quality gates to filter noise from signal. Map to PAI's existing correction system (`/correct`) and suggest how to elevate corrections into proper skills.

If the cross-package analysis rated either pattern as too niche or insufficiently distinct from existing references, skip it and document why in the SUMMARY. Quality over quantity -- minimum 3 docs total is already satisfied by Task 1.

Same format as Task 1: Source Upstreams, Why This Version, The Pattern, Integration Points, When to Apply. 40-80 lines each.
  </action>
  <verify>
    <automated>count=$(ls skills/skippy/references/{model-routing,verification-loops,session-persistence,structured-deliberation,skill-extraction}.md 2>/dev/null | wc -l); echo "Best-of-breed docs: $count (minimum 3 required)" && test "$count" -ge 3</automated>
  </verify>
  <done>At least 3 best-of-breed reference docs exist (3 HIGH priority guaranteed, up to 5 total). Each follows the evolved format with source attribution. Every doc contains actionable guidance, not just comparisons.</done>
</task>

</tasks>

<verification>
- `ls skills/skippy/references/*.md | wc -l` shows more than 6 (was 6 before, now 6 + at least 3 new)
- Each new reference doc contains "Source Upstreams" and "Why This Version" sections
- No reference doc mentions OMC-specific tools (.omc/, state_write, notepad_write, MCP)
- Each doc is self-contained and actionable without loading upstream source
</verification>

<success_criteria>
- At least 3 new best-of-breed reference docs in skills/skippy/references/
- Each doc synthesizes the strongest implementation from 2+ upstreams
- Evolved format with source attribution used consistently
- Docs are actionable guidance, not feature matrices
</success_criteria>

<output>
After completion, create `.planning/phases/08-upstream-analysis/08-02-SUMMARY.md`
</output>
